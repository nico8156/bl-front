import { Route } from "@angular/router";
import { BookInfoComponent } from "./component/bookInfo.component";

export const routes: Route[] = [
    {
        path: '',
        component: BookInfoComponent
    }
]