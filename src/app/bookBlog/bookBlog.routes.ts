import { Route } from "@angular/router";
import { BookBlogComponent } from "./component/bookBlog.component";

export const routes: Route[] = [
    {
        path: '',
        component: BookBlogComponent,
    }
]